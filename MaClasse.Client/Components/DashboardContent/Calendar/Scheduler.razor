@using MaClasse.Shared.Models.Scheduler
@using MaClasse.Client.Components.Utils
@using Variant = MudBlazor.Variant

<div class="scheduler-view">
    <div class="scheduler-toolbar">

        <div class="scheduler-toolbar-row">
            <MudButton StartIcon="@Icons.Material.Filled.Today"
                       aria-label="Revenir à la date d'aujourd'hui"
                       title="Aujourd'hui"
                       Variant="Variant.Filled"
                       Color="Color.Primary"
                       OnClick="@GoToToday">
                Aujourd'hui
            </MudButton>

            <div class="scheduler-view-switch">
                <MudButtonGroup Color="Color.Info"
                                Variant="Variant.Outlined"
                                aria-label="Choix de la vue du calendrier">
                    <MudIconButton Icon="@Icons.Material.Filled.Today"
                                   aria-label="Afficher la vue jour"
                                   aria-pressed="@(selectedViewIndex == 0)"
                                   title="Vue jour"
                                   Color="Color.Info"
                                   OnClick="@(() => SetSchedulerViewAsync(0))" />

                    <MudIconButton Icon="@Icons.Material.Filled.ViewWeek"
                                   aria-label="Afficher la vue semaine"
                                   aria-pressed="@(selectedViewIndex == 1)"
                                   title="Vue semaine"
                                   Color="Color.Info"
                                   OnClick="@(() => SetSchedulerViewAsync(1))" />
                </MudButtonGroup>
            </div>
        </div>

        <div class="scheduler-toolbar-row">
            <MudIconButton Icon="@Icons.Material.Filled.DateRange"
                           aria-label="Ouvrir le sélecteur de date"
                           title="Choisir une date"
                           Color="Color.Primary"
                           OnClick="@(() => datePickerOpen = true)" />

            <MudIconButton Icon="@Icons.Material.Filled.ArrowBackIosNew"
                           aria-label="Aller à la période précédente"
                           title="Période précédente"
                           Color="Color.Info"
                           OnClick="@GoToPrevious" />

            <MudText Typo="Typo.body1"
                     Class="@($"scheduler-date-label{(selectedViewIndex == 1 ? " scheduler-date-label--week" : string.Empty)}")">
                @DisplayedDateLabel
            </MudText>

            <MudIconButton Icon="@Icons.Material.Filled.ArrowForwardIos"
                           aria-label="Aller à la période suivante"
                           title="Période suivante"
                           Color="Color.Info"
                           OnClick="@GoToNext" />

            <MudIconButton Icon="@Icons.Material.Filled.PostAdd"
                           aria-label="Ajouter un rendez-vous"
                           title="Ajouter un rendez-vous"
                           Disabled="_schedulerState.isReadOnly"
                           Color="Color.Success"
                           OnClick="() => OpenNewAppointment()" />
        </div>
    </div>

    @if (showContextMenu)
    {
        <MudPaper id="custom-context-menu"
                  Class="mud-elevation-8"
                  Style="@($"position:fixed; top:{menuY}px; left:{menuX}px; z-index:9999; min-width: 150px; padding: 0.5rem; background: white; border-radius: 4px; box-shadow: 0px 2px 8px rgba(0,0,0,0.2);")"
                  @onmousedown:stopPropagation>
            <MudList T="object">
                <MudListItem T="object" OnClick="@(() => { OpenLessonForSelectedAppointment(); CloseCustomMenu(); })">
                    <MudText>Afficher</MudText>
                </MudListItem>
                <MudListItem T="object" Disabled="@isReadOnly" OnClick="@(() => { OpenEditPanelForSelectedAppointment(); CloseCustomMenu(); })">
                    <MudText>Modifier</MudText>
                </MudListItem>
            </MudList>
        </MudPaper>
    }

    @if (datePickerOpen)
    {
        <div class="scheduler-floating-panel">
            <MudPaper>
                <MudDatePicker PickerMonth="_currentMonth"
                               Label="Sélectionner une date"
                               Date="@currentDate"
                               PickerVariant="PickerVariant.Static"
                               DateChanged="OnDatePicked" />
                <MudButton Variant="Variant.Text" Color="Color.Info" OnClick="@(() => datePickerOpen = false)">Fermer</MudButton>
            </MudPaper>
        </div>
    }

    @if (showAppointmentPanel)
    {
        <div class="scheduler-floating-panel">
            <AddAppointmentPage Model="@selectedAppointment"
                                Start="@selectedStart"
                                End="@selectedEnd"
                                OnSave="OnAppointmentSaved"
                                OnCancel="ClosePanel"
                                IsEditMode="@isEditMode"
                                OnDelete="OnAppointmentDeleted" />
        </div>
    }

    <div class="scheduler-canvas">
        <RadzenScheduler @ref="@scheduler"
                         @bind-Date="currentDate"
                         SelectedIndex="@selectedViewIndex"
                         SlotRender=@OnSlotRender
                         style="height: 100%; width: 100%;"
                         TItem="Appointment"
                         Data=@appointments
                         StartProperty="Start"
                         EndProperty="End"
                         ShowHeader="false"
                         TextProperty="Text"
                         SlotSelect=@OnSlotSelect
                         AppointmentSelect=@OnAppointmentSelect
                         AppointmentRender=@OnAppointmentRender
                         DaySelect="@OnDaySelect"
                         AppointmentMove=@OnAppointmentMove>
            <RadzenDayView StartTime="@startTime" EndTime="@endTime" TimeFormat="HH:mm" />
            <RadzenWeekView StartTime="@startTime" EndTime="@endTime" TimeFormat="HH:mm" />
        </RadzenScheduler>
    </div>
</div>
